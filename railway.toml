[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn legacylens.api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
